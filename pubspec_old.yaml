name: aura_app
description: "AURA - Aplicación para Parejas a Distancia con transparencia voluntaria y presencia digital sin vigilancia"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    firebase_core: ^3.6.0
    firebase_messaging: ^15.1.3
    permission_handler: ^11.3.1



  # Core Dependencies
  cupertino_icons: ^1.0.8

  # Backend - Appwrite (latest version)
  appwrite: 17.0.0

  # State Management
  provider: ^6.1.2

  # UI & Animations
  animated_text_kit: ^4.2.2
  flutter_animate: ^4.5.0
  flutter_staggered_animations: ^1.1.1

  # Local Storage & Cache
  shared_preferences: ^2.3.4
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Connectivity & Network
  connectivity_plus: ^6.1.4
  dio: ^5.7.0

  # Notifications
  flutter_local_notifications: ^19.3.0

  # Utils & Helpers
  intl: ^0.20.2
  uuid: ^4.5.1
  equatable: ^2.0.5
  json_annotation: ^4.9.0

  # Device Features - Compatible with Appwrite 17.0.0
  vibration: ^3.1.3
  device_info_plus: ^10.1.2
  package_info_plus: ^8.3.0

  # Permissions
  permission_handler: ^12.0.1
  flutter_riverpod: ^2.6.1
  dartz: ^0.10.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting & Code Quality
  flutter_lints: ^6.0.0

  # Code Generation - Compatible versions
  build_runner: ^2.4.8
  json_serializable: ^6.8.0
  hive_generator: ^2.0.1

  # Testing - Compatible versions
  mockito: ^5.4.4
  bloc_test: ^9.1.7

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
